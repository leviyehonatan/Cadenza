# Build output
build/
build-*/
out/
probe_out/
style-scan-report.csv
genos-scan.csv
graphify-out/
dist/

# Large SoundFonts are distributed via the package script, not git
# (GeneralUser GS is small enough to stay tracked).
resources/sf2/Timbres Of Heaven*.sf2

# Yamaha style files are copyrighted user-supplied content — never commit them.
*.STY
*.PRS
*.SST
*.FPS
*.BCS
*.sty
*.prs
*.sst
*.fps
*.bcs

# CMake
CMakeCache.txt
CMakeFiles/

# Compiled / linker artifacts
*.obj
*.exe
*.ilk
*.pdb
*.lib
*.exp

# IDE
.vs/
.vscode/
*.user
*.suo
*.VC.db
*.VC.opendb

# Dependencies (all vendored: JUCE clone, Steinberg ASIO SDK, etc. — never committed)
lib/

# Logs
*.log

# Generated diagnostics + tooling artifacts (regenerated on demand)
diagnostics/
.superpowers/

# Bundled third-party plugins / neural models (not part of the source build)
*.vst3
NeuralPi/
dnnn Audio/
VST3/
uploads/

# Legacy web-UI prototype — superseded by the native JUCE UI (JUCE_WEB_BROWSER=0)
Cadenza Workstation.html
css/
js/

# Internal AI-assistant config + study / audit docs (not for the public repo)
.claude/
CLAUDE.md
*_STUDY.md
FEATURES_AGENTS_README.md
DEPENDENCY_FEATURE_AUDIT.md
LIVE_PLAYBACK_VS_EXPORT_AUDIT.md
docs/superpowers/
